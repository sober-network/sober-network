export const environment = {
  production: true,
  // In production the Angular SPA is served by the same ASP.NET Core process,
  // so API calls go to the same origin — no host needed.
  apiUrl: '',
};
